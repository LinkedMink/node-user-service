#!/usr/bin/env groovy

node {
  stage('Checkout Source Code') {
    checkout scm
  }
  stage('Build and Push Docker Image') {
    echo 'Build Docker Image'
  }
  stage('Deploy Image to Environment: Development') {
    echo 'Deploy Image to Environment: Development'
  }
}
